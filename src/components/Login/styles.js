import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    justifyContent: 'center',
    alignItems: 'center',
    flex: 1
  },
  inputBox: {
    height: 40,
    width: 120,
    borderColor: 'gray',
    borderWidth: 1,
    margin: 5
  }
})
